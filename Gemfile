source "https://rubygems.org"

# GitHub Pages stack: pins Jekyll + plugins (incl. jekyll-remote-theme)
# to exactly what GitHub Pages runs, so local builds match production.
gem "github-pages", group: :jekyll_plugins

# Windows / JRuby friendliness (harmless elsewhere)
gem "webrick", "~> 1.8"
